# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- Static HTML observatory at `docs/index.html`: single file, vanilla JS + inline SVG, light theme, monospace-heavy "Grafana-style" layout. Default-loads `demo_trace.json` from the same directory; drag-and-drop or file-picker for ad-hoc traces (read locally via `FileReader`, never uploaded). Pipeline diagram with status-coloured arrows (green pass / amber warn / red block), per-handoff modal with source→target field diff (equal / changed / dropped / added), per-rule and per-field score tables, baked impact summary, and event metadata. Accessibility: skip link, ARIA roles, focus trap inside modal, ESC closes, keyboard-activatable handoff rows. Schema-version guard surfaces a warning banner when a loaded trace declares anything other than `schema_version: "0.1"` so future bumps don't silently mis-render (PRC-022).
- Committed `docs/demo_trace.json` snapshot regenerable via `python examples/demo.py --output docs/demo_trace.json`; lets reviewers and the GitHub Pages deploy render the observatory without a Python runtime (PRC-022).
- Browser-driven Playwright snapshot tests at `Dev/tests/integration/observatory/test_observatory.py` covering default trace rendering, modal diff content for the failed hop, ESC-close behaviour, and the schema-version mismatch banner. Opt-in via the new `e2e` pytest marker and `[e2e]` optional extra; the default `pytest` run skips them via `addopts = ["-m", "not e2e"]` so contributors avoid the ~250 MB Chromium download by default (PRC-022).
- `docs/404.html` minimal landing page with a 5-second `<meta refresh>` back to the observatory root and a fallback link to the repo (PRC-023).
- `.github/workflows/pages.yml` publishes `docs/` to GitHub Pages on pushes to `main` and on manual dispatch. Pure static-file copy: no runtime Python, so a broken import on `main` cannot break the public observatory URL. Pre-deploy `Verify required artefacts are committed` step asserts `docs/index.html`, `docs/404.html`, and `docs/demo_trace.json` exist and the trace is at `schema_version: "0.1"`. Workflow header carries a `MANUAL STEP REQUIRED` comment noting the one-time repo-settings flip (Settings → Pages → Source → "GitHub Actions") needed before the first deploy succeeds; the workflow itself is intentionally idempotent across retries (PRC-023).
- Live observatory link and badge in the root `README.md` pointing at `https://gianpremrajaram.github.io/precept/` (PRC-023).
- Initial repository scaffolding: PEP 621 `pyproject.toml`, `src/precept/` package layout, `__version__` sourced via `importlib.metadata`, `test`, `docs`, and `dev` optional-dependency groups, and a root `.gitignore` covering Python, IDE, and OS artefacts (PRC-001).
- Continuous integration pipeline: `.github/workflows/ci.yml` with `lint` (ruff check + format), `typecheck` (mypy --strict), `security` (bandit), and `test` (pytest with coverage) jobs; test job matrixes across Python 3.10 / 3.11 / 3.12 on `ubuntu-latest`, other jobs pin to 3.12; per-job `working-directory: Dev`; coverage uploaded as a job artefact (PRC-003).
- Tool configuration in `pyproject.toml`: `[tool.ruff]`, `[tool.mypy]` (strict), `[tool.pytest.ini_options]`, and `[tool.coverage]` so CI commands stay thin and behaviour lives with the package config (PRC-003).
- CI status badge at the top of `README.md` (PRC-003).
- Smoke test `Dev/tests/test_smoke.py` verifying the installed package exposes a non-empty `__version__` (PRC-003).
- `CONTRIBUTING.md` at repo root: concise external-facing guide (<150 lines) covering dev setup, branch naming (`feat/`, `fix/`, `docs/`, `infra/`, `test/`, `research/`), Conventional Commits, PR flow, and pre-commit install commands; links out to `CLAUDE.md`, `ISSUES.md`, and `DEPENDENCIES.md` (PRC-002).
- `.github/PULL_REQUEST_TEMPLATE.md` with Summary / Linked issue / Testing / Checklist sections (PRC-002).
- `.github/ISSUE_TEMPLATE/bug_report.md` and `.github/ISSUE_TEMPLATE/feature_request.md` (PRC-002).
- LICENSE copyright year verified as 2026 (PRC-002).
- `.pre-commit-config.yaml` at repo root: file-hygiene (`trailing-whitespace`, `end-of-file-fixer`, `check-yaml`, `check-toml`, `check-merge-conflict`, `detect-private-key`) and `ruff` + `ruff-format` on commit; `mypy --strict` on pre-push to keep the commit loop fast. Ruff is pointed at `Dev/pyproject.toml` via `--config` for reliable discovery from repo-root invocation. Hook scope is repo-wide (PRC-004).
- ADR 0001 (`docs/adr/0001-contract-ir.md`) documenting the single-Pydantic-IR, multiple-frontends architecture with Mermaid + ASCII diagrams, non-goals (no runtime codegen, no dynamic schema extension, opaque version semantics), and explicit empty-contract (scaffold / observe-only) semantics; ADR index at `docs/adr/README.md`; linked from `README.md` and `CONTRIBUTING.md` (PRC-005).
- `precept.errors` module with `ContractValidationError` and structured `ContractValidationIssue` (Pydantic) carrying `field_path`, `message`, and optional `yaml_mark`; `ContractValidationError.from_pydantic` classmethod wraps `pydantic.ValidationError` uniformly (PRC-006, shared home for future `HandoffBlockedError` in PRC-013).
- `precept.contract.schema` with `HandoffContract` and `ContractFields` Pydantic v2 models; `extra="forbid"`, `name` regex (`^[a-z][a-z0-9_-]*$`), `mode: Literal["block", "warn"]`, bounded `min_fidelity` required when `preserved_entities` is non-empty, `required_fields`/`forbidden_drops` overlap rejection, list-duplicate rejection; `HandoffContract.__init__` wraps `pydantic.ValidationError` into `ContractValidationError`. Empty contracts (all three rule lists empty) are permitted as a deliberate scaffold mode per ADR 0001 (PRC-006).
- Shared Hypothesis strategies at `Dev/tests/unit/contract/strategies.py` (`valid_contract_names`, `valid_min_fidelity`, `valid_field_names`) reusable by PRC-008 and PRC-012 (PRC-006).
- Unit tests at `Dev/tests/unit/contract/test_schema.py` covering 38 cases including the required-when-preserved, overlap, duplicate, and property-based paths (PRC-006).
- `precept.contract.yaml_loader` with `load_contract(str | Path) -> HandoffContract` and `load_contract_from_string(str) -> HandoffContract`; uses `yaml.safe_load` exclusively, surfaces `(line, column)` on syntax errors via `ContractValidationIssue.yaml_mark`, rejects empty / non-mapping / unknown-key YAML with actionable messages; supports YAML anchors and aliases natively (PRC-007).
- Example contract YAML files at repo root: `examples/contracts/researcher_to_summariser.yaml` (blocking, fidelity 0.75) and `examples/contracts/summariser_to_writer.yaml` (warn, fidelity 0.70, mode defaulted) (PRC-007).
- Unit and integration tests for the YAML loader (`tests/unit/contract/test_yaml_loader.py`, `tests/integration/test_yaml_loader_integration.py`) covering example-file round-trip, anchor/alias resolution, malformed YAML with line/column, semantic round-trip, and contract distinctness (PRC-007).
- Runtime dependencies `pydantic>=2.5,<3` and `pyyaml>=6.0,<7` in `Dev/pyproject.toml`; dev additions `pip-tools>=7.4,<8` and `types-PyYAML>=6.0,<7` for lockfile generation and strict-mode type checking (PRC-006, PRC-007).
- `Dev/requirements-dev.lock` generated via `pip-compile --extra dev` for reproducible dev environments; regenerate on any dependency change per CLAUDE.md.
- Three follow-up tickets in `ISSUES.md`: PRC-006a (JSON Schema export helper), PRC-007a (YAML loader error enrichment with line/column on schema violations), PRC-007b (`examples/contracts/README.md`); all P2, post-MVP.
- `precept.scoring.base` module with the four anchor types of the scoring layer: `HandoffPayload` (Pydantic; `fields: dict[str, Any]`, optional `raw`, string-keyed `metadata`, `extra="forbid"`), `FieldScore` (per-field result with bounded `score in [0,1]`, `method`, `passed`), `ScoreResult` (aggregate `overall_score in [0,1]`, `field_scores`, `scorer_name`, `scorer_version`, `timestamp_iso`), and the `Scorer` ABC with the abstract `score(source, target, contract) -> ScoreResult` method (PRC-010).
- `Scorer.__init_subclass__` enforcement: every concrete subclass must declare non-empty class-level `name: str` and `version: str` attributes; failure raises `TypeError` at class-definition time. Intermediate abstract subclasses (those still carrying unimplemented abstract methods) are exempted via `inspect.isabstract`, so layered hierarchies remain possible (PRC-010).
- `Scorer` docstring codifies the implementation contract for `score()` (determinism, non-mutation of inputs, tolerance for missing optional fields, thread-safety) and reaffirms the constructor-time-load discipline. The interface is deliberately method-agnostic: no mutual-information vocabulary in the ABC, so the Phase 2 `CalibratedScorer` (PRC-035) drops in without an interface break (PRC-010).
- `ScoreResult.timestamp_iso` field validator enforcing UTC-offset ISO 8601 strings: rejects naive datetimes and non-UTC offsets, normalises a trailing `Z` suffix to `+00:00` so behaviour is consistent across the `>=3.10` Python floor (Python 3.10's `datetime.fromisoformat` does not accept `Z`; 3.11+ does). Locks the timestamp convention down before PRC-013's `ViolationEvent` introduces a sibling field (PRC-010).
- Unit tests at `Dev/tests/unit/scoring/test_base.py` (35 cases) covering ABC instantiation guard, `__init_subclass__` enforcement (missing `name`, missing `version`, empty string, non-string), intermediate-abstract exemption, positive subclass round-trip, `HandoffPayload` value polymorphism + extra-field rejection, `FieldScore` and `ScoreResult` range validation including boundary values, `timestamp_iso` UTC-offset validation (`+00:00` accepted, `Z` normalised, naive rejected with explicit message, non-UTC offset (`+01:00`) rejected, garbage rejected), and module `__all__` sanity (PRC-010).
- `precept.scoring.embedding_proxy.EmbeddingProxy`: v0 concrete `Scorer`. Sentence-transformers cosine-similarity proxy on `all-MiniLM-L6-v2` by default (`paraphrase-MiniLM-L3-v2` and arbitrary HF model identifiers swappable via `model_name`). Per-field cosine on `contract.fields.required_fields`; unweighted-mean `overall_score`; missing target field scores `0.0` with `passed=False`; non-string field values stringified via `str()`; `method="embedding_cosine"` on every `FieldScore`; clamps cosine to `[0,1]` to absorb floating-point and adversarial-pair edges (PRC-011).
- Constructor-time model load with INFO log of the resident-memory footprint constant (no `psutil` dependency); `torch.set_grad_enabled(False)` set at construction for inference-only operation (PRC-011).
- `_skip_model_load: bool = False` test-only escape hatch on `EmbeddingProxy.__init__`: skips model load and causes `score()` to raise `RuntimeError` with explicit guidance. Used by unit tests to exercise scorer wiring without the ~80 MB `all-MiniLM-L6-v2` download. Documented as private; not part of the public API (PRC-011).
- Empty-`required_fields` contracts (ADR 0001 scaffold/observe-only mode) score `overall_score=1.0` with `field_scores=[]`. Vacuous pass aligns with PRC-013's `all(field_score.passed)` aggregation (PRC-011).
- Scorer-level `min_fidelity` fallback: when `contract.fields.min_fidelity` is `None` and `required_fields` is non-empty, `EmbeddingProxy` applies a `0.5` cosine threshold and logs a `WARNING` once per `score()` call. The default lives in the scorer, not in `ContractFields`, to keep the IR generic for the Phase 2 `CalibratedScorer` (PRC-035): a calibrated MI scorer would pick its own threshold from a calibrated probability distribution, where `0.5` cosine has no meaning. See `embedding_proxy` module docstring (PRC-011).
- Unit tests at `Dev/tests/unit/scoring/test_embedding_proxy.py` (18 cases): identical-near-perfect (≥0.99), unrelated-low (<0.3), missing-target zero, field-count match, timestamp UTC-offset round-trip, empty-required vacuous pass with no warning, `min_fidelity=None` warning + 0.5 fallback (passing and failing branches), non-string and list-of-string stringification, determinism over 10 runs, `paraphrase-MiniLM-L3-v2` model-swap, `_skip_model_load` `RuntimeError`, async-via-`asyncio.to_thread` non-blocking smoke (sync API safely dispatchable from coroutines; native-async deferred to Phase 2). Integration test at `Dev/tests/integration/scoring/test_embedding_proxy_integration.py` exercising a degraded handoff against a 0.75-fidelity contract (PRC-011).
- Runtime dependencies `sentence-transformers>=2.2,<4` and `numpy>=1.24,<3` in `Dev/pyproject.toml`; narrow `[[tool.mypy.overrides]] ignore_missing_imports = true` for `sentence_transformers.*` and `torch.*` (no global escape hatch). `Dev/requirements-dev.lock` regenerated via `pip-compile pyproject.toml --extra dev` (PRC-011).
- `precept.evaluator` package scaffold (`Dev/src/precept/evaluator/__init__.py`) with empty `__all__`. Submodule names import directly until PRC-026 publishes the public surface (PRC-012).
- `precept.evaluator.rules` module exposing the three v0 rule evaluators as pure module-level functions: `required_fields_rule`, `preserved_entities_rule`, `forbidden_drops_rule`. Each is stateless, side-effect-free, and never raises - rule failures are returned as `RuleResult` data so the PRC-013 `Evaluator` aggregates them without exception-flow plumbing (PRC-012).
- `RuleResult` Pydantic model: `rule_name: str`, `passed: bool`, `details: dict[str, Any]` (default empty), `violation_message: str | None`. `ConfigDict(extra="forbid")`. `violation_message` is `None` on pass and human-readable on fail; per-rule `details` shape is documented in each rule's docstring and treated as a public contract for PRC-013's `ViolationEvent` renderer (PRC-012).
- `required_fields_rule(source, target, required)`: presence check on `target.fields`. `details = {"required": list[str], "missing": list[str]}` preserving the `required` order. `source` is part of the uniform rule signature but unused; documented in the docstring (PRC-012).
- `preserved_entities_rule(source, target, entities)`: case-insensitive substring matching on the newline-joined stringified values of `HandoffPayload.fields` (NOT `.raw`; `.raw` is optional and would couple rule behaviour to producer-population habits). Vacuous-pass when an entity is absent from source - mirrors `forbidden_drops_rule` semantics. NER-based matching deferred to PRC-030 (PRC-012).
- `forbidden_drops_rule(source, target, forbidden)`: structural drop check; flags fields present in `source.fields` but absent in `target.fields`. Target-only fields pass. `details = {"forbidden": list[str], "dropped": list[str]}` (PRC-012).
- Unit tests at `Dev/tests/unit/evaluator/test_rules.py` (29 cases): per-rule positive/negative/empty-list paths, single- and multi-missing message templates (singular vs plural form, including the verbatim AC exemplar `"Required field 'hypothesis' missing in target payload"`), case-insensitive entity matching in both source-upper/target-lower and source-lower/target-upper directions, vacuous-pass for entities absent from source, non-string field stringification, `.raw`-not-consulted assertion, no cross-field-boundary substring collisions, target-only and absent-from-both branches for forbidden drops, `RuleResult` `extra="forbid"` rejection. Hypothesis property tests: rules never raise on arbitrary input (random unicode keys, mixed-type values), and `required_fields_rule(any, target_with_those_fields)` always passes (PRC-012).
- `[tool.pytest.ini_options] pythonpath = ["tests/unit/contract"]` in `Dev/pyproject.toml` so the shared `valid_field_names` Hypothesis strategy in `tests/unit/contract/strategies.py` (originally placed there with PRC-012 named in its docstring as a reuse target) is importable from any test module without sys.path manipulation in conftest (PRC-012).
- `precept.types.ViolationEvent` Pydantic v2 model (`extra="forbid"`) carrying every field a downstream consumer needs to reconstruct an evaluation: `contract_name`, `contract_version`, `mode: Literal["block","warn"]`, `passed`, `score_result: ScoreResult`, `rule_results: list[RuleResult]`, `triggered_at_iso`, `event_id` (UUID4 default factory), `source_summary` / `target_summary: dict[str, str]`, and `schema_version: Literal["0.1"]`. The `triggered_at_iso` validator mirrors `ScoreResult._validate_timestamp_iso_utc` exactly (rejects naive and non-UTC offsets; normalises trailing `Z` to `+00:00` for the Python 3.10 `fromisoformat` floor) (PRC-013).
- `ViolationEvent.to_compact_dict() -> dict[str, str | int | float | bool]`: flat OTel-attribute layout under the `precept.*` namespace (no nested dicts at the top level) with a hard 4 KiB ceiling on the JSON-serialised byte length. Under size pressure, target-summary entries are dropped first (longest value first), then source-summary entries; a `precept.payload_truncated: True` flag is set on entry to the truncation loop, so it surfaces both the "we successfully truncated" and the irreducible-overflow ("we tried but the base shape exceeds the budget") cases. Setting the flag is idempotent so the ~36 flag bytes are paid exactly once across the whole loop. The 4 KiB target is the realistic OTel-attribute lower bound across backends (Datadog, Jaeger), so silently-dropped events are replaced with explicitly-flagged events that backends can detect and surface (PRC-013).
- `precept.errors.HandoffBlockedError(Exception)` with `violation_event: ViolationEvent` and `impact_summary: str = ""`. `impact_summary` is mutable by design: PRC-015's impact populator writes onto an existing instance immediately before re-raise, and PRC-014's integration layer is the only caller that constructs and re-raises this class. The mutability is a deliberate, documented contract that breaks the original PRC-014 ⇄ PRC-015 circular dependency without forcing the error class to depend on the impact-template module. A subprocess regression test asserts that `from precept.errors import HandoffBlockedError` does not transitively load `precept.evaluator.engine` or `precept.scoring.embedding_proxy` (PRC-013).
- `precept.exporters` package scaffold (`Dev/src/precept/exporters/__init__.py`) with empty `__all__`. Submodule names import directly until PRC-026 publishes the public surface, mirroring the `precept.evaluator` package convention (PRC-013).
- `precept.exporters.base.Exporter` ABC with abstract `export(event: ViolationEvent) -> None` and a `name: ClassVar[str]` requirement enforced by `__init_subclass__` (mirrors the `Scorer` pattern, including the `inspect.isabstract` exemption for intermediate abstract subclasses). Class docstring codifies the implementation contract: must not raise on transport failure (log + continue), must not mutate `event`, must be safe to call from multiple threads, and should be best-effort idempotent on repeat `event_id` (PRC-013).
- `precept.exporters.base.NoOpExporter` (`name="noop"`): default safe sink; `export()` is a no-op. Used by `Evaluator` when no exporter is supplied and as a baseline test fixture (PRC-013).
- `precept.exporters.base.MultiExporter` (`name="multi"`): fan-out exporter with per-child failure isolation. `export()` iterates children in declaration order; a `RuntimeError` from one child is logged and swallowed so siblings still receive the event. Empty child list is allowed (silent success) (PRC-013).
- `precept.evaluator.engine.Evaluator`: composes the three rule evaluators (PRC-012) with the injected `Scorer` (PRC-010) and `Exporter` (PRC-013) into a single `evaluate(source, target, contract) -> ViolationEvent` call. `passed` is the conjunction of every `RuleResult.passed` and every `FieldScore.passed`; `mode` is metadata. The evaluator never raises on a contract violation - the integration layer (PRC-014) decides whether to raise `HandoffBlockedError`. Exporter failures are isolated via try/except so an unconfigured or misbehaving exporter never starves the caller of its event. `source_summary` / `target_summary` are built from `payload.fields` with a 100-char-per-value upstream truncation (the 4 KiB compact-dict ceiling is the secondary bound) (PRC-013).
- Thread-safety contract on `Evaluator` (class docstring): one instance is safe to call from multiple threads, given the `Scorer` and `Exporter` contracts. Future caching or per-call mutation on `self` would break this and must update the contract loudly (PRC-013).
- Unit tests at `Dev/tests/unit/test_types.py` (21 cases): construction shape and `extra="forbid"` rejection, `event_id` uniqueness across constructions and round-trip when supplied explicitly, `triggered_at_iso` validator parity with `ScoreResult.timestamp_iso` (UTC offset, `Z` suffix, naive rejection, non-UTC rejection, garbage rejection), `to_compact_dict` leaf-scalar values, JSON round-trip, conditional inclusion of per-rule `violation_message`, no-truncation flag on normal events, 4 KiB ceiling and `payload_truncated` flag under inflated summaries, target-before-source drop order, both-sides-dropped degenerate path, irreducible-overflow termination + flag (50 rule_results with no summaries push the base shape past 4 KiB; the loop hits `break` without an infinite loop and the flag is still set so backends can detect the over-budget event), and a schema-drift canary that asserts `set(ViolationEvent.model_fields)` matches a hard-coded set plus that every expected `precept.*` prefix appears in `to_compact_dict()` keys (so a future field rename without a flattener update fails this test loudly) (PRC-013).
- Unit tests at `Dev/tests/unit/test_errors.py` (7 cases): `ContractValidationError` regression import, `HandoffBlockedError` event carrier, default empty `impact_summary`, constructor-kwarg path, post-construction-mutation pattern with documented contract, exception message naming the contract, and a subprocess leaf-import regression that asserts no edge from `precept.errors` to `precept.evaluator.engine` or `precept.scoring.embedding_proxy` (the cycle-break invariant) (PRC-013).
- Unit tests at `Dev/tests/unit/exporters/test_exporters_base.py` (12 cases): ABC instantiation guard, `__init_subclass__` enforcement (missing/empty/non-string `name` rejected at class definition; intermediate-abstract subclasses exempt; concrete subclass with valid name succeeds), `NoOpExporter` no-op behaviour and `name` value, `MultiExporter` fan-out order, per-child failure isolation with WARNING log, empty-list silent-success, and continue-after-first-failure correctness with multiple failing children. Filename is namespaced (`test_exporters_base.py`, not `test_base.py`) to avoid pytest module-basename collision with `tests/unit/scoring/test_base.py` (PRC-013).
- Unit tests at `Dev/tests/unit/evaluator/test_engine.py` (15 cases) using a deterministic `StubScorer` (no embedding download): default `NoOpExporter`, explicit-exporter and explicit-scorer accessors, clean handoff passing, required-field-missing failure with verbatim AC violation message, preserved-entity-dropped failure, forbidden-drop failure, `FieldScore.passed=False` flipping aggregate `passed` even when all rules pass, 100 distinct `event_id`s across consecutive evaluations, summary 100-char truncation, non-string field stringification, default-exporter no-raise, recording-exporter receipt, failing-exporter isolation with WARNING log, and `MultiExporter([failing, working])` delivering to working despite the first child failing (PRC-013).
- Integration test at `Dev/tests/integration/evaluator/test_engine_integration.py` (3 cases): wires the real `EmbeddingProxy` into `Evaluator(NoOpExporter())` against a `min_fidelity=0.75`, `required_fields=["hypothesis", "citations"]` contract; asserts a clean (identical-payload) handoff yields `event.passed=True`, a degraded handoff (faithful hypothesis paraphrase + gutted citations) yields `event.passed=False` with the citations field score failing, and the resulting `to_compact_dict()` is <= 4 KiB JSON-serialised. Inline payloads (no PRC-018 dependency) follow the existing PRC-011 integration-test convention (PRC-013).
- `precept.integrations` and `precept.integrations.langgraph` package scaffolds (`Dev/src/precept/integrations/__init__.py`, `.../langgraph/__init__.py`) with empty `__all__`; submodule names import directly until PRC-026 publishes the public surface, mirroring the `precept.evaluator` / `precept.exporters` convention (PRC-016).
- `precept.integrations.langgraph.extractor.extract_payload(state, contract, *, max_field_chars=2000) -> HandoffPayload`: reads ONLY `contract.fields.required_fields ∪ preserved_entities` (de-duplicated, first-seen order) from arbitrary upstream state; no recursive descent, no PII heuristic, no stringification of non-string values. Mapping-shaped state resolves via `Mapping.get`, object-shaped state via `getattr`; a private `_MISSING` sentinel distinguishes an absent field (recorded as literal `None`, increments `missing_field_count`) from a field present with value `None` (recorded as `None`, counted as extracted). The two-branch resolver replaces the AC's literal `state[field]` step because a bare subscript raises `KeyError` on a missing key, colliding with the missing-as-`None` requirement; rationale in the module docstring (PRC-016).
- String values longer than `max_field_chars` are truncated with a `... [truncated N chars]` suffix where `N` is the character count removed; non-string and nested-object values pass through by identity. `HandoffPayload.metadata` carries `extraction_method` (`Literal["mapping","attribute"]`, stored as `str`), `extracted_field_count`, `missing_field_count`, `truncated_field_count` (counts stringified because `HandoffPayload.metadata` is `dict[str, str]`); `truncated_field_count` is the structured truncation signal for machine consumers (PRC-016).
- Unit tests at `Dev/tests/unit/integrations/langgraph/test_extractor.py` (18 cases): dict / dataclass / Pydantic / `SimpleNamespace` state shapes, missing-vs-present-`None` distinction, custom-`max_field_chars` truncation with verbatim suffix, non-string and nested-object identity pass-through, union dedup + first-seen order, metadata-count consistency, empty (scaffold) contract, the gating secret-leakage regression (uncontracted secrets / message-like objects never reach `fields`, `metadata`, or `model_dump_json()`), and two Hypothesis property tests asserting `len(fields) == |required ∪ preserved|` for arbitrary dict states (AC) and the attribute branch (deliberate over-AC hardening, flagged in-test). The LangGraph `HumanMessage` case uses a local stand-in to avoid adding `langgraph` as a runtime dependency (PRC-014 scope) (PRC-016).
- `precept.contract.decorator.handoff_contract`: sync-only decorator frontend (ADR 0001's second frontend). Produces the same `HandoffContract` IR as the YAML loader by routing a nested `fields` mapping through `HandoffContract.__init__`, so schema violations surface as `ContractValidationError` at decoration time (not call time). Ergonomic kwarg `required` maps to IR `required_fields`. An optional first-positional sentinel detects the bare `@handoff_contract` misuse and raises `TypeError` with guidance rather than Python's native positional-argument error; omitting the required `name` raises `TypeError` (an API-usage error kept categorically distinct from a contract-content `ContractValidationError`); coroutine functions raise `TypeError` with the documented Phase-2 redirect. `functools.wraps` preserves the wrapped signature; the validated contract is attached as `__precept_contract__` by attribute assignment with a targeted `type: ignore[attr-defined]` (mypy-strict and ruff clean) for the PRC-014 integration layer to consume. No call-time evaluation at v0 (PRC-008).
- `examples/decorator_usage.py`: self-contained runnable example (no `langgraph` import) demonstrating decoration, `__precept_contract__` retrieval, and unchanged call-time behaviour (PRC-008).
- Unit tests at `Dev/tests/unit/contract/test_decorator.py` (8 cases: signature/return/docstring preservation, attached-IR field mapping, schema-default fallthrough, decoration-time `ContractValidationError` for invalid `min_fidelity` and invalid `name`, async-function, bare-form, and missing-`name` `TypeError` with verbatim messages) and an integration test at `Dev/tests/integration/contract/test_decorator_yaml_equivalence.py` asserting decorator/YAML IR equality (fully specified + defaults-only) with explicit per-field diagnostics including `metadata == {}` on both frontends (PRC-008).
- `precept.contract.registry` with `ContractRegistry` (`register` / `get` / `list_contracts` / `load_directory`) and a replaceable module-level `default_registry` (a plain attribute, not a Singleton). `register` is `threading.Lock`-guarded and raises `KeyError` on duplicate name unless `overwrite=True`; `get` raises `KeyError` listing available names (`Available: a, b`, or `(none)`); `list_contracts` returns sorted names. `load_directory(path, *, recursive=False)` globs `*.yaml` (non-YAML excluded by the glob), skips malformed/invalid files with a `WARNING` caught at the `ContractValidationError` boundary the YAML loader already wraps to (no `yaml.YAMLError` leakage), registers with `overwrite=True` so re-loading a directory is idempotent, and returns the registered count (PRC-009).
- Unit tests at `Dev/tests/unit/contract/test_registry.py` (10 cases: register/retrieve, sorted listing, duplicate-raise + overwrite, missing-name message with and without available contracts, `default_registry` type, multi-file count, non-YAML + broken-syntax + schema-invalid skip with two logged warnings and no raise, recursion, idempotent re-load) (PRC-009).
- `precept.integrations.langgraph.impact`: the violation-impact populator. `IMPACT_TEMPLATES: dict[tuple[str, str], str]` is a curated `(contract_name, failing_rule_name) -> impact_text` table (Python dict, not YAML, so a contract rename surfaces as a test-time missing key rather than a silent fallthrough); `render_impact_text(contract_name, rule_name, source_agent, target_agent) -> str` is the pure templating helper (reusable by the PRC-022 observatory without constructing an error); `populate_impact_summary(error) -> None` performs the single bounded post-construction write onto `HandoffBlockedError.impact_summary` described in the PRC-013 error contract, reading but never mutating the attached `ViolationEvent`. `DEFAULT_IMPACT_FALLBACK` is a named constant rendered when no template matches. Agent names are derived from the `<source>_to_<target>` contract-name convention (`str.partition` on the first `_to_`, so a nested-infix target is preserved), falling back to generic `upstream`/`downstream` when the infix is absent, because neither `ViolationEvent` nor `HandoffContract` carries agent identity and the populator signature receives only the error. The first `passed is False` rule (evaluator order) is the explained failure; a pure sub-threshold field-score block (no rule failed, or an empty `rule_results`) is labelled `minimum_fidelity` which, not being a template key, renders the fallback. Hand-written placeholder per ISSUES.md Q8; learned impact prediction is Phase 2 (PRC-015).
- Unit tests at `Dev/tests/unit/integrations/langgraph/test_impact.py` (12 cases, 100% statement+branch coverage of `impact.py`): every template renders without leaking the fallback, in-place mutation with the attached event left byte-for-byte identical (`model_dump` round-trip), unknown combination falls back, rendered string carries all four tokens with the exact AC format, demo-omission regression over the four PRC-018 combinations, agent derivation from the contract name plus nested-infix and no-infix fallback branches, pure score-gate and empty-`rule_results` `minimum_fidelity` paths, first-failing-rule selection in evaluator order, and an `__all__` sorted/complete canary (PRC-015).
- Follow-up ticket in `ISSUES.md`: PRC-015a (score-gate impact text names the failing field(s)); P2, post-MVP. The convention-based agent-name parsing is logged as a v0 simplification in DEPENDENCIES.md section 10 (PRC-015).
- `precept.integrations.langgraph.eval_hook.evaluate_handoff(source, target, contract_name, *, registry=None, evaluator=None, raise_on_block=True) -> ViolationEvent`: the pure, framework-API-independent handoff hook for the `Command(goto=...)` pattern. Imports no `langgraph` symbol. Resolves the contract from `registry` (defaults to `default_registry`), builds source/target `HandoffPayload`s via the PRC-016 extractor, runs `Evaluator.evaluate` (dispatching to the configured exporter), and raises `HandoffBlockedError` — with the PRC-015 impact summary populated immediately before the raise — only when `raise_on_block` and `mode == "block"` and `not event.passed`. Fail-open on a missing contract: logs `WARNING`, returns a synthetic *pass* event (sentinels `contract_version="unknown"`, `mode="warn"`, `scorer_name="synthetic"`), never raises, never exports. Async-safe: a running event loop is detected via `asyncio.get_running_loop()` and the CPU-bound `evaluate` is offloaded to a worker thread; the fully non-blocking idiom is documented as `await asyncio.to_thread(evaluate_handoff, ...)` (PRC-014).
- Lazy module-cached default evaluator: when no `evaluator` is passed, `Evaluator(EmbeddingProxy())` is built on the *first* call (lock-guarded, double-checked) and cached — never at import, so importing the hook never triggers the ~80 MB model load. This defers *construction of the default instance*, not the in-`__init__` weight load, so the CLAUDE.md constructor-time-load discipline is preserved; mirrors the `default_registry` precedent (PRC-014).
- `precept.integrations.langgraph.handoff_tool.create_precept_handoff_tool(agent_name, contract_name, *, registry=None, evaluator=None, name=None, description=None) -> BaseTool`: drop-in contract-checking handoff tool for tool-calling supervisors. Uses the manual `InjectedState` + `InjectedToolCallId` + `Command(goto=..., graph=Command.PARENT)` pattern (the documented `langchain-ai/langgraph-supervisor-py#205` workaround); a block-mode failure raises `HandoffBlockedError`, which LangGraph's `ToolNode` surfaces to the supervisor LLM as an error `ToolMessage`. Built on `evaluate_handoff` so only this module is exposed to LangGraph tool-API churn (PRC-014).
- `precept.integrations.langgraph.__init__` now exports `evaluate_handoff` and `create_precept_handoff_tool` via PEP 562 `__getattr__` (lazy): importing `evaluate_handoff` never imports `langgraph`, preserving the pure hook as the framework-independent fallback while satisfying the PRC-014 AC. This supersedes the prior empty-`__all__` scaffold note for these two names only; PRC-026 still owns the top-level `precept` namespace (PRC-014).
- Runtime dependencies `langgraph>=0.5,<0.7` and `langchain-core>=0.3,<1` added to `Dev/pyproject.toml` (DEPENDENCIES.md section 3.1). `langgraph-supervisor` is deliberately NOT a dependency (not even test-only): the tool wrapper implements the manual `langchain-ai/langgraph-supervisor-py#205` pattern on core `langgraph`, and `langgraph_supervisor.create_supervisor` cannot adopt a custom handoff tool without that library's private `"__handoff_destination"` marker, which Precept does not couple to (DEPENDENCIES.md sections 4.1, 10). Narrow `[[tool.mypy.overrides]]` with `follow_imports = "skip"` treats the HIGH-instability `langgraph` / `langchain_core` surface as untyped, install-independently. `Dev/requirements-dev.lock` regenerated via `pip-compile --extra dev` (PRC-014).
- Unit tests `Dev/tests/unit/integrations/langgraph/test_eval_hook.py` (10 cases, no `langgraph` import: valid-pass, block-raises with impact-summary asserted, warn-emits-no-raise, missing-contract fail-open, `raise_on_block=False`, in-running-loop returns event, `to_thread` does-not-starve-loop tick smoke per the PRC-011 precedent, lazy default-evaluator built-once, `default_registry` fallback, `__all__` canary) and `Dev/tests/unit/integrations/langgraph/test_handoff_tool.py` (4 cases, langgraph/langchain faked in `sys.modules`: evaluates on invocation, handoff preserved on pass, block surfaces as raise, custom name/description) (PRC-014).
- Integration tests `Dev/tests/integration/integrations/langgraph/test_langgraph_integration.py` (4 cases, `importorskip`-gated CI drift detector per DEPENDENCIES.md section 4.1: real `StateGraph` + `Command` pass and degraded-block paths, async-node `ainvoke` no-RuntimeWarning, and `create_precept_handoff_tool` driven end to end through the real pinned `langgraph` `ToolNode` / `Command(graph=PARENT)` / `InjectedState` machinery in the manual tool-calling-supervisor topology it is designed for — not `langgraph_supervisor.create_supervisor`, which silently shadows unmarked custom handoff tools; see DEPENDENCIES.md sections 4.1, 10). Skipped (not errored) where `langgraph` is absent — e.g. the maintainer's interpreter outside the 3.10–3.12 matrix; executed on the CI matrix (PRC-014).
- Demo trace fixtures `examples/fixtures/clean_trace.json` and `examples/fixtures/degraded_trace.json`: two-hop `researcher -> summariser -> writer` pipeline modelling a UK heat-pump adoption research question (realistic content per ISSUES.md PRC-017 AC: hypothesis, citations, primary source, author, uncertainty bounds, and summariser-stage `summary` + `key_entities`). Each hop carries `handoff_metadata` (`source_agent`, `target_agent`, `contract_name`, `timestamp_iso`), `source_payload`, and `target_payload`. The clean trace's payloads are identical across each handoff (every contract passes); the degraded trace keeps hop 0 clean and degrades hop 1 by removing the `primary_source` and `uncertainty_bounds` fields from the target — the dropped `primary_source` value carried the contract's preserved domain entity `MCS Quarterly Statistics`, so the target's stringified blob loses that substring and `preserved_entities_rule` fires while `forbidden_drops_rule` fires on the missing `uncertainty_bounds`. Every `FieldScore` still passes the 0.70 fidelity gate (PRC-017).
- `Dev/tests/conftest.py`: `clean_trace` and `degraded_trace` pytest fixtures load the JSON files committed under `examples/fixtures/`. Placed at the tests root (not `tests/fixtures/conftest.py` as the AC literally reads) because pytest only auto-discovers conftests in ancestor directories of a test file, and `tests/integration/test_fixtures.py` is a sibling of any `tests/fixtures/` location; raising the conftest one level is the minimal correct placement. Deviation rationale recorded in the conftest docstring (PRC-017).
- Unit tests `Dev/tests/unit/fixtures/test_fixtures_schema.py` (6 cases): each fixture parses against an inline `TraceFixture(BaseModel)` (with `HandoffMetadata` and `Hop` submodels, all `extra="forbid"`), `trace_name` matches the expected `clean` / `degraded` literal, the agent-sequence is `researcher -> summariser -> writer`, and every `contract_name` resolves to an existing YAML under `examples/contracts/`. Filename is namespaced (not `test_schema.py`) to avoid pytest module-basename collision with `tests/unit/contract/test_schema.py`, matching the PRC-013 `test_exporters_base.py` precedent (PRC-017).
- Integration test `Dev/tests/integration/test_fixtures.py` (2 cases): module-scoped `Evaluator(EmbeddingProxy(), NoOpExporter())` runs the real scoring stack against both fixtures with contracts loaded from `examples/contracts/`. Asserts the clean trace produces zero violations across both hops and the degraded trace fails exactly the summariser->writer hop with rule failures `{forbidden_drops, preserved_entities}` and zero score-gate failures. Module docstring notes that fixtures and contracts are co-designed: `preserved_entities` lists domain strings (e.g. `"MCS Quarterly Statistics"`) that live verbatim in source payload values, so dropping the carrying field naturally fires the rule (PRC-017).
- `examples/demo.py`: single-file CLI runner that loads a fixture trace, evaluates each hop directly through `Evaluator.evaluate` + `EmbeddingProxy` + `NoOpExporter` (no LangGraph involvement — PRC-019 AC is explicit that this is the contract-engine demo, not the runtime demo; LangGraph end-to-end coverage lives in `Dev/tests/integration/integrations/langgraph/`), prints a per-hop summary to stdout, and writes a deterministic JSON trace the PRC-022 observatory will render. CLI surface: `--trace {clean|degraded}` (default `degraded`), `--output PATH` (default `examples/output/demo_trace.json`), `--fixtures-dir DIR` / `--contracts-dir DIR` (default to the committed `examples/fixtures` and `examples/contracts` directories resolved from `__file__`, so cwd does not matter), `--no-color` (force ANSI off; otherwise gated on `sys.stdout.isatty()`). Uses `argparse` only — no `click`, `typer`, `rich`, or other deps (PRC-019).
- Output JSON schema (`examples/output/demo_trace.json`): top-level `schema_version` (`"0.1"`), `precept_version` (from `importlib.metadata`), `trace_name`, `generated_at_iso` (the last hop's fixture timestamp, stable run-to-run), and `hops: list`. Each hop carries `handoff_metadata` (echoed from the fixture), `source_payload` / `target_payload` (echoed from the fixture so the observatory can render per-field comparisons without re-fetching — the violation event's `source_summary` / `target_summary` are truncated to 100 chars/field, insufficient for a useful UI), `violation_event` (`event.model_dump(mode='json')` with deterministic substitutions), and `impact_summary: str | None` (rendered for failed hops, `None` for passing hops). Written via `json.dump(..., indent=2, sort_keys=True)` plus trailing newline, so output is `diff`-friendly and the byte-identical-repeat AC holds (PRC-019).
- Determinism scheme: three `ViolationEvent` fields are stamped by `datetime.now(timezone.utc)` or `uuid.uuid4()` at evaluation time and vary run-to-run — `event_id`, `triggered_at_iso`, and the nested `score_result.timestamp_iso`. The runner overwrites these in the *serialised* dict (the live `ViolationEvent` is never mutated) with stable values: `event_id = "demo-" + sha256(f"{trace_name}:{hop_index}:{contract_name}").hexdigest()[:24]`, both timestamps replaced with the hop's fixture `timestamp_iso`. The `demo-` prefix makes the synthetic id obviously non-UUID4 so a downstream tool will not mistake it for a real evaluation id. Embedding-cosine scores are themselves deterministic on a single machine (`EmbeddingProxy` runs sentence-transformers with grad disabled and frozen weights) and need no fixing (PRC-019).
- Impact-summary baking for PRC-022 observatory coordination (per ISSUES.md PRC-022 "Impact copy provenance"): for each failed hop the runner calls `render_impact_text(contract_name, failing_rule_name, source_agent, target_agent)` directly from `precept.integrations.langgraph.impact` and writes the rendered string into the hop's `impact_summary`. The failing-rule name is the first `RuleResult.passed is False` (mirrors `impact._failing_rule_name`), falling back to the `"minimum_fidelity"` sentinel for pure score-gate failures (which intentionally hits `DEFAULT_IMPACT_FALLBACK`). Source / target agent names come from the fixture's `handoff_metadata`, not from `impact._agents_from_contract_name`'s contract-name `_to_` split: the demo authoritatively knows the agent names so it sidesteps that helper's documented v0 weakness for source names embedding `_to_` (tracked in DEPENDENCIES.md section 10) (PRC-019).
- Exit codes (PRC-019 AC, Unix convention; explicitly NOT 0-or-1-on-violations): `0` on completed run regardless of whether the trace contained violations (a degraded trace producing violations IS a successful demo run; violations are surfaced via stdout and the JSON output, never via the exit code — a reviewer typing `python examples/demo.py` and seeing exit `1` would assume a crash); `2` on any uncaught exception (fixture missing, contract unloadable, output path unwritable). Runtime errors print a `DEMO FAILED: <Type>: <message>` line on stderr instead of a Python traceback. Argparse-level argument errors retain argparse's own exit `2` for free. Rationale documented in the module docstring so a future contributor does not "fix" the convention back to 0-or-1-on-violations (PRC-019).
- Console output: ASCII banner (`Precept v<version> -- Handoff Integrity Demo`); one-line model-load progress message printed *before* the per-hop section so the multi-second sentence-transformer cold-load does not look like a stalled handoff; per-hop block showing source -> target, contract name, overall score (3 decimals), `PASS` / `FAIL` status, per-rule `✓`/`✗` summary, the failing rule's `violation_message` on failure, and the baked impact line; final summary with totals, elapsed seconds, output path, and the verbatim observatory pointer line `Open docs/index.html and drop examples/output/demo_trace.json to view details`; closing `DEMO COMPLETED (no violations)` / `DEMO COMPLETED (1 violation detected)` / `DEMO COMPLETED (N violations detected)` (correctly singular/plural). `PASS` is rendered green and `FAIL` red via ANSI; both are gated on `sys.stdout.isatty() and not --no-color` so piped/captured output stays clean (PRC-019).
- `examples/output/.gitkeep`: tracked empty file so a fresh clone has the documented default output directory in place; `demo.py` still calls `mkdir(parents=True, exist_ok=True)` on the output's parent before writing so a user-supplied `--output` to any missing directory works the same way (PRC-019).
- Integration tests at `Dev/tests/integration/test_demo.py` (12 cases, subprocess-driven): `_run_demo` invokes `examples/demo.py` via `sys.executable` (so the demo runs in the test's venv) with `--no-color` and a 120 s timeout. Module-scoped `demo_clean` / `demo_degraded` fixtures run each trace once and cache the `(CompletedProcess, output_path, parsed_json)` tuple, so the 12 tests trigger 4 total `EmbeddingProxy` loads (one each for clean, degraded, degraded-repeat in the determinism test, plus the runtime-error case that fails before model load). Cases cover: `returncode == 0` on both clean and degraded (regression catcher for the 0-or-1-on-violations anti-pattern), zero violations on clean with `impact_summary is None` on every hop, exactly the summariser->writer hop failing on degraded with rule failures `{preserved_entities, forbidden_drops}`, baked `impact_summary` carrying the curated PRC-015 `(summariser_to_writer, preserved_entities)` template plus the fixture's agent names (proves the impact line is not the generic fallback and that agents flow through `handoff_metadata`), the verbatim `DEMO COMPLETED (...)` summary lines for both branches, the observatory pointer string in stdout, the top-level + per-hop JSON key sets, a `json.dumps(parsed, indent=2, sort_keys=True) + "\n"` round-trip equality check that catches any regression in indent / sort_keys / key order / trailing newline, a deterministic-repeat check that runs the degraded trace twice into distinct tmp paths and asserts the output files are byte-identical, and a runtime-error check that points `--fixtures-dir` at a nonexistent directory and asserts `returncode == 2` with `DEMO FAILED` on stderr (PRC-019).
- `precept.exporters.json_exporter.JSONFileExporter` (`name="json_file"`) and `precept.exporters.json_exporter.JSONStreamExporter` (`name="json_stream"`): the v0 backup / optionality exporters. Both inherit from `precept.exporters.base.Exporter`, write one event per line as JSON Lines via `event.model_dump(mode="json")` (full event detail, not the 4 KiB-capped `to_compact_dict()` -- file storage is not size-constrained), serialise concurrent writes within a single process via `threading.Lock`, and swallow transport failures (disk full, permission denied, broken pipe / closed stream) as WARNING logs per the `Exporter` contract. `JSONFileExporter` creates its parent directory at construction (predictable error surface at app startup) and, in `mode="overwrite"`, eagerly truncates the destination file at construction so the destination is in a known state before any evaluation runs; each `export()` then re-opens in append mode (no idle handle), which keeps the surface friendly to log-rotation tools. Multi-process safety (e.g. two Gunicorn workers writing the same file) is out of v0 scope and documented per-class: callers needing it use external coordination (`fcntl.flock`) or per-worker output paths. `JSONStreamExporter` holds the supplied writable text stream by reference; lifecycle (open/close) stays the caller's responsibility (PRC-021).
- Unit tests at `Dev/tests/unit/exporters/test_json.py` (17 cases): JSONL line shape and `\n` termination, one-event-per-line, top-level `schema_version` carried verbatim from `ViolationEvent` (no exporter-level augmentation), parent-directory auto-creation including nested paths, `mode="append"` extension of a pre-existing file, `mode="overwrite"` truncation at construction time (asserting the pre-existing line is gone before any `export()`) and the subsequent fresh-append behaviour, in-process concurrent-write integrity across 8 threads × 25 events (no inter-line interleaving), stream-variant `io.StringIO` round-trip, file-OSError swallowing (monkey-patched `Path.open` to raise `OSError("disk full")`; assertion: WARNING log + no raise), stream-closed `ValueError` swallowing, and an `OSError` on `stream.write` (broken-pipe simulation) (PRC-021).
- `precept.exporters.otel.OTelExporter` (`name="otel"`): OpenTelemetry GenAI exporter (PRC-020). Emits one `gen_ai.evaluation.result` event per `ViolationEvent`, following the OTel GenAI semantic conventions (status: Development / experimental as of April 2026; DEPENDENCIES.md §4.3). Constructor parameters: optional `tracer` (`opentelemetry.trace.Tracer`; defaults to `trace.get_tracer(__name__)`) and optional `event_logger` (`opentelemetry._events.EventLogger`; opted in by passing it). Both OTel API and SDK are opt-in via the new `[otel]` extra (`pip install precept[otel]`); the module-level import is guarded so base installs import the module cleanly, and the stub `OTelExporter` raises `ImportError("OTelExporter requires the 'otel' extra. Install via: pip install precept[otel]")` on both `__init__` and `export()` (the second is a defensive backstop for code paths that bypass `__init__`). Stable emission path: `span.add_event("gen_ai.evaluation.result", attributes=...)` on the current recording span; when no active recording span exists, the exporter opens a synthetic `invoke_agent` span via the configured tracer, attaches the event, and ends the span (PRC-020 AC). Experimental emission path: `EventLogger.emit(Event(name=..., attributes=...))` when `event_logger` is supplied (opt-in). Attribute set combines `ViolationEvent.to_compact_dict()` (Precept-namespaced, already 4 KiB-capped by `precept.types`) with the GenAI canonical names `gen_ai.evaluation.name`, `gen_ai.evaluation.score.value`, `gen_ai.evaluation.score.label` (`"passed"` / `"failed"`), and `gen_ai.evaluation.explanation` (rule violation messages joined with `;`, or a generic "evaluation failed: see precept.score.* attributes for per-field detail" fallback on pure score-gate failures). Content-capture gate: `precept.source.*` and `precept.target.*` attributes are stripped before emission unless `OTEL_INSTRUMENTATION_GENAI_CAPTURE_MESSAGE_CONTENT=True` is set (case-insensitive match on `"true"`); this defends against payload-content leaks to telemetry backends. Per-attribute size backstop: any single string attribute exceeding 4 KiB UTF-8 is byte-sliced to fit (with `errors="ignore"` on decode for multi-byte boundary safety) and suffixed with `"...[truncated]"`; this is the second defence behind the event-level `to_compact_dict()` ceiling (DEPENDENCIES.md §4.4). `OTEL_SEMCONV_STABILITY_OPT_IN` is read on every `export()` and logged at DEBUG -- v0 emits only the experimental GenAI schema (the only one in scope), so the env var has no behavioural branch yet; the integration point is wired in so a future contributor finds it already in place when OTel renames an attribute (DEPENDENCIES.md §4.3 emit-then-migrate plan). Failure isolation: SDK not configured (global no-op `TracerProvider`) is logged at DEBUG with "no OTel SDK configured; gen_ai.evaluation.result event dropped" (so misconfiguration is detectable without paging); any unexpected exception from the tracer or event logger is also DEBUG-logged and swallowed per the `Exporter` contract (PRC-020).
- Unit tests at `Dev/tests/unit/exporters/test_otel.py` (18 cases): `name="otel"` assertion, default-tracer ProxyTracer construction, explicit-tracer round-trip, GenAI canonical keys present (`name`, `score.value`, `score.label`, `explanation`), pass-vs-fail label routing, score-gate-only explanation fallback, content-capture stripping by default, content-capture inclusion when env var set (uppercase `"True"` and lowercase `"true"` both accepted), `precept.payload_truncated` propagation when `to_compact_dict` truncated upstream, per-attribute 4 KiB truncation helper, current-recording-span attachment path (verifies the event lands on the parent span via `InMemorySpanExporter` and no synthetic span is created), synthetic-`invoke_agent`-span path when no active span (verifies exactly one new span named `"invoke_agent"` carrying the event), SDK-not-configured DEBUG log without raise, broad tracer-exception swallowing (custom exploding tracer; assertion: DEBUG log "export failed" + "tracer broke"), `OTEL_SEMCONV_STABILITY_OPT_IN` DEBUG log when set, EventLogger preference when supplied (asserts the experimental path bypasses span creation entirely), and a subprocess-driven import-guard probe that blocks `opentelemetry.*` in `sys.modules` before importing `precept.exporters.otel`, asserts the module still imports, asserts `OTelExporter()` raises `ImportError` with the install hint, and asserts `OTelExporter.__new__(OTelExporter).export(None)` (bypassing `__init__`) also raises the same `ImportError` (PRC-020).
- Integration tests at `Dev/tests/integration/exporters/test_otel_integration.py` (2 cases, `importorskip`-gated on `opentelemetry.sdk`): module-scoped `EmbeddingProxy` + per-test `TracerProvider` with `SimpleSpanProcessor(InMemorySpanExporter())`; runs a clean handoff and a degraded handoff against a `min_fidelity=0.75`, `required_fields=["hypothesis", "citations"]` contract; asserts the captured spans match expected shape (one synthetic `invoke_agent` span per run, carrying a `gen_ai.evaluation.result` event with the GenAI canonical attribute set and no `precept.source.*` / `precept.target.*` leakage by default). End-to-end coverage that the exporter wires correctly through the real Evaluator + EmbeddingProxy stack with a real `TracerProvider` (PRC-020).
- New `precept[otel]` optional-dependency group in `Dev/pyproject.toml`, bundling `opentelemetry-api>=1.30,<2` and `opentelemetry-sdk>=1.30,<2`. Both ship together so an `[otel]`-extras install can construct a real `OTelExporter` immediately; base installs stay free of OTel surface. `Dev/requirements-dev.lock` regenerated via `pip-compile pyproject.toml --extra dev --extra otel -o requirements-dev.lock`. New `[[tool.mypy.overrides]] module = ["opentelemetry.*"] ignore_missing_imports = true` so strict type-checking stays green on base installs where the optional surface is not importable; runtime usage in `precept.exporters.otel` wraps every OTel symbol behind the module-level import guard plus `TYPE_CHECKING`-only annotations (`from __future__ import annotations` keeps the runtime free of type-import overhead). DEPENDENCIES.md §3.1 / §3.2 updated to match: `opentelemetry-api` is documented as moved into the `[otel]` extra rather than the originally-planned runtime row (PRC-020).

### Changed
- Relicensed from MIT to Apache-2.0. `LICENSE` replaced with the canonical Apache 2.0 text (apache.org/licenses/LICENSE-2.0.txt) with the appendix copyright filled in; `Dev/pyproject.toml` `license` field and PyPI classifier updated; the `# SPDX-License-Identifier:` header on every Python file under `Dev/` and `examples/` flipped to `Apache-2.0` (56 files). New repo-root `NOTICE` file carries the project attribution per Apache convention and a third-party acknowledgement for `CLAUDE.md`, which adapts behavioural-discipline principles from [`multica-ai/andrej-karpathy-skills`](https://github.com/multica-ai/andrej-karpathy-skills) (itself derived from Andrej Karpathy's observations on LLM coding pitfalls). New repo-root `DCO` file carries the canonical Developer Certificate of Origin v1.1 text; contributions now require a `Signed-off-by` trailer (`git commit -s`), enforced by the new `.github/workflows/dco.yml` workflow. `CONTRIBUTING.md` gains a `Sign-off (DCO)` section and the PR template gains a sign-off checklist item. README badge row picks up an Apache-2.0 shield. No code behaviour changes; no contributor consent required (sole-author at flip time; pre-PyPI, pre-tag).
- `examples/contracts/summariser_to_writer.yaml` now declares `mode: block` and `forbidden_drops: [uncertainty_bounds]` to match PRC-018's demo-contract specification, the rule-firing pattern of `degraded_trace.json`, and the `(summariser_to_writer, forbidden_drops)` impact template already curated in `precept.integrations.langgraph.impact` (PRC-015). The previous warn-mode, no-forbidden-drops shape lived only as an unreleased PRC-007 starter. The accompanying `Dev/tests/unit/contract/test_yaml_loader.py::test_summariser_to_writer_loads_with_default_mode` was renamed to `test_summariser_to_writer_loads_with_block_mode_and_forbidden_drops` and its assertions updated; `Dev/tests/integration/test_yaml_loader_integration.py` drops the `c1.mode != c2.mode` distinctness check (both example contracts are now block-mode) and continues to assert distinctness on `name`, `required_fields`, and `min_fidelity` (PRC-018).
- Both `examples/contracts/*.yaml` files now declare `preserved_entities` as domain strings rather than field names, matching the documented semantics of `preserved_entities_rule` (case-insensitive substring match on stringified field VALUES, designed for entities like `"Smith"`). `researcher_to_summariser.yaml` now preserves `["MCS Quarterly Statistics", "Boiler Upgrade Scheme"]`; `summariser_to_writer.yaml` now preserves `["MCS Quarterly Statistics"]`. The previous field-name entries (`primary_source`, `author`) caused `preserved_entities_rule` to pass vacuously because field names never appear inside stringified field values; the demo fixtures had to inject `"primary_source: MCS Quarterly Statistics Q4 2025"` into `key_entities` as a workaround. That workaround is removed in the same change. `Dev/tests/unit/contract/test_yaml_loader.py` and `Dev/tests/integration/test_fixtures.py` updated to assert the new domain-string entities (PRC-017, PRC-018).
- Repository flipped to public and accompanying release-status signals added: README now carries a research-preview callout under the badges, a `Status: research preview` badge alongside the existing CI and Pages badges, and an expanded `## Status` component-state table enumerating which surfaces are working, in development, and planned. New repo-root `SECURITY.md` declares the vulnerability-reporting flow (GitHub private vulnerability reporting with an email fallback), scope, and pre-1.0 support policy. New `.github/CODEOWNERS` assigns `@gianpremrajaram` as the default owner so external PRs auto-request the maintainer's review.

### Fixed
- Six pre-existing `mypy --strict` errors in the test suite, surfaced by running the strict gate over `tests/` for the first time (the existing gate was scoped to `src/precept` only): four `__precept_contract__` `attr-defined` errors in `Dev/tests/unit/contract/test_decorator.py` and `Dev/tests/integration/contract/test_decorator_yaml_equivalence.py` (matched the source-side `# type: ignore[attr-defined]` already present in `precept.contract.decorator`), one `func-returns-value` in `Dev/tests/unit/exporters/test_exporters_base.py::test_noop_exporter_export_returns_none` (the assertion was tautological because `NoOpExporter.export` is typed to return `None`; replaced with a plain call), and one `unused-ignore` in `Dev/tests/unit/contract/test_schema.py` (`# type: ignore[dict-item]` no longer needed). `mypy --strict src/precept tests` now reports zero issues across 47 files. Surgical-edits scope only; no logic changes (PRC-017, PRC-018).

### Deferred
- `CODE_OF_CONDUCT.md` body and reporting-contact email: tracked as new ticket **PRC-002a** (pre-public-release gate). Committing a real contact address to a public git history is irreversible, so this is parked until a public contact alias or GitHub private-reporting flow is chosen. PRC-002's DoD item referring to the CoC is explicitly deferred to PRC-002a in `ISSUES.md`.
